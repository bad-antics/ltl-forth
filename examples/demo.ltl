// examples/demo.ltl — runnable demo for ltl_forth

import std::io
use ltl_forth as lib

fn main() {
    io::println("ltl_forth demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}